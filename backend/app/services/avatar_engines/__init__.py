"""Concrete `AvatarEngine` implementations. See `services/avatar_engine.py`
for the abstraction every implementation here honors, and
`docs/AVATAR_ARCHITECTURE.md` for which one is active and why."""
