# Saudi Arabic TTS Prototype — environment template.
# Copy to .env and fill in. NEVER commit .env.
# This phase's one provider (Groq) requires a credential — unlike the prior
# feature, there is no credential-free default path here.

GROQ_API_KEY=
